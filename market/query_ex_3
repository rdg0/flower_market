create temp table combined_temp as

SELECT app_city.name, COUNT(*) AS qt
FROM app_person
JOIN app_city ON app_city.id=app_person.city_id
GROUP BY app_city.name

SELECT name, MAX(qt) as qt
FROM combined_temp
UNION 
SELECT name, MIN(qt) as qt
FROM combined_temp
ORDER BY name DESC
